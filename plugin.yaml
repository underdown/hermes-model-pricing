name: pricing-tools
version: "1.0.0"
description: "Fetch live model pricing and list available models across providers — surfaced as /pricing and /models slash commands on Discord, Telegram, and other gateways."
author: underdown
provides_tools:
  - fetch_pricing
  - list_models
kind: standalone